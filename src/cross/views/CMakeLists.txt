# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

cmake_minimum_required(VERSION 3.19)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
	message(FATAL_ERROR "In-tree builds are not supported. Run CMake from a separate directory: cmake -B build")
endif()

set(CMKR_ROOT_PROJECT OFF)
if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
	set(CMKR_ROOT_PROJECT ON)

	# Enable folder support
	set_property(GLOBAL PROPERTY USE_FOLDERS ON)

	# Create a configure-time dependency on cmake.toml to improve IDE support
	set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS cmake.toml)
endif()

project(machdbg_views
	LANGUAGES
		CXX
)

# Target: machdbg_register_format
set(machdbg_register_format_SOURCES
	RegisterFormat.cpp
	RegisterFormat.h
	cmake.toml
)

add_library(machdbg_register_format STATIC)

target_sources(machdbg_register_format PRIVATE ${machdbg_register_format_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${machdbg_register_format_SOURCES})

target_compile_features(machdbg_register_format PUBLIC
	cxx_std_17
)

target_include_directories(machdbg_register_format PUBLIC
	"../"
	"../MachBug"
)

# Target: machdbg_views
set(machdbg_views_SOURCES
	BreakpointTable.cpp
	BreakpointTable.h
	EngineMemoryPage.cpp
	EngineMemoryPage.h
	EngineMemoryProvider.cpp
	EngineMemoryProvider.h
	MemoryMapTable.cpp
	MemoryMapTable.h
	ModuleTable.cpp
	ModuleTable.h
	RegisterTable.cpp
	RegisterTable.h
	ThreadTable.cpp
	ThreadTable.h
	cmake.toml
)

add_library(machdbg_views STATIC)

target_sources(machdbg_views PRIVATE ${machdbg_views_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${machdbg_views_SOURCES})

target_compile_features(machdbg_views PUBLIC
	cxx_std_17
)

target_include_directories(machdbg_views PUBLIC
	"../"
	"../MachBug"
)

target_link_libraries(machdbg_views PUBLIC
	x64dbg::widgets
	machdbg_register_format
)

set(CMKR_TARGET machdbg_views)
set_target_properties(machdbg_views PROPERTIES AUTOMOC ON)
